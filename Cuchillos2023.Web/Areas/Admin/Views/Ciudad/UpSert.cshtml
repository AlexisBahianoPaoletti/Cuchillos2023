@using Cuchillos2023.Web.ViewModels
@model CiudadEditVm
@{
    ViewData["Title"] = "UpSert";
}

<form method="post" asp-action="UpSert">
    <input type="hidden" asp-for="Ciudad.Id" />

    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Nueva Ciudad</h2>
        </div>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="mb-3">
            <label asp-for="Ciudad.NombreCiudad"></label>
            <input asp-for="Ciudad.NombreCiudad" class="form-control" />
            <span asp-validation-for="Ciudad.NombreCiudad" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Ciudad.ProvinciaId" class="form-label"></label>
            <select asp-for="Ciudad.ProvinciaId" asp-items="Model.ProvinciasList" class="form-select">
                <option disabled selected>--Seleccionar Provincia--</option>
            </select>
            <span asp-validation-for="Ciudad.ProvinciaId" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary" style="width:200px">
            <i class="bi bi-floppy"></i> &nbsp;
            Guardar
        </button>
        <a asp-controller="Ciudad" asp-action="Index" style="width:200px" class="btn btn-success">
            <i class="bi bi-backspace"></i>  &nbsp;
            Volver
        </a>
    </div>
</form>
@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}
